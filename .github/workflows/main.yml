name: Run Playwright API Call

on:
  workflow_dispatch:   # Trigger manually

jobs:
  call-api:
    runs-on: ubuntu-latest
    steps:
      - name: Call Playwright Run API
        run: |
          curl --location 'https://cd44673ad17e.ngrok-free.app/api/v1/playwright/run-tests-ciCD?id=286&email=finalPlanCheck@yopmail.com'
